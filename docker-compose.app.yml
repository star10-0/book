services:
  migrate:
    image: book-app:latest
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env.production
    environment:
      NODE_ENV: production
    command: ["npm", "run", "prisma:migrate:deploy"]
    restart: "no"

  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: book-app:latest
    env_file:
      - .env.production
    environment:
      NODE_ENV: production
    command: ["npm", "run", "start"]
    depends_on:
      migrate:
        condition: service_completed_successfully
    ports:
      - "${APP_PORT:-3000}:3000"
    volumes:
      - ${PUBLIC_UPLOADS_PATH:-./volumes/public-uploads}:/app/public/uploads
      - ${PRIVATE_UPLOADS_PATH:-./volumes/private-uploads}:/app/storage/private/uploads
    restart: unless-stopped
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "node -e \"require('http').get('http://127.0.0.1:3000/api/health', (res) => process.exit(res.statusCode >= 500 ? 1 : 0)).on('error', () => process.exit(1))\"",
        ]
      interval: 15s
      timeout: 5s
      retries: 10
